{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../Phonebook-frontend && npm run build && cp -r dist ../PhoneBook",
    "deploy:full": "npm run build:ui && git add dist/ && git commit -m \"automated frontend production build\" && git push origin main"
  },
  "author": "Pawan Hada",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
